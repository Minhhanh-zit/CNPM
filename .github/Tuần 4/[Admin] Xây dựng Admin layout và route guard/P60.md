# Issue #60 - [Admin] Xây dựng Admin layout và route guard

## 1. Mục tiêu

Xây dựng giao diện quản trị cho hệ thống đặt vé xem phim, bao gồm layout admin, sidebar điều hướng, dashboard admin và cơ chế route guard để chỉ tài khoản có quyền `ADMIN` mới được truy cập trang quản trị.

Issue này thuộc issue mẹ `#40`.

---

## 2. Danh sách file cần tạo/cập nhật

```txt
src/
├── layouts/
│   └── AdminLayout.tsx
│
├── components/
│   └── admin/
│       └── AdminSidebar.tsx
│
├── pages/
│   └── admin/
│       ├── AdminDashboardPage.tsx
│       ├── AdminMoviesPage.tsx
│       ├── AdminShowtimesPage.tsx
│       └── AdminBookingsPage.tsx
│
├── routes/
│   └── AdminRouteGuard.tsx
│
├── App.tsx
└── styles/
    └── admin.css
```

---

## 3. Code chi tiết

### 3.1. `src/routes/AdminRouteGuard.tsx`

```tsx
import { Navigate, Outlet } from "react-router-dom";

type User = {
  id?: number | string;
  fullName?: string;
  email?: string;
  role?: string;
};

function getCurrentUser(): User | null {
  const rawUser = localStorage.getItem("user");

  if (!rawUser) {
    return null;
  }

  try {
    return JSON.parse(rawUser) as User;
  } catch {
    return null;
  }
}

export default function AdminRouteGuard() {
  const user = getCurrentUser();

  if (!user) {
    return <Navigate to="/login" replace />;
  }

  if (user.role !== "ADMIN") {
    return <Navigate to="/" replace />;
  }

  return <Outlet />;
}
```

---

### 3.2. `src/components/admin/AdminSidebar.tsx`

```tsx
import { NavLink } from "react-router-dom";

export default function AdminSidebar() {
  return (
    <aside className="admin-sidebar">
      <div className="admin-sidebar__logo">
        <h2>Movie Admin</h2>
        <span>Quản trị hệ thống</span>
      </div>

      <nav className="admin-sidebar__nav">
        <NavLink
          to="/admin"
          end
          className={({ isActive }) =>
            isActive ? "admin-sidebar__link active" : "admin-sidebar__link"
          }
        >
          Dashboard
        </NavLink>

        <NavLink
          to="/admin/movies"
          className={({ isActive }) =>
            isActive ? "admin-sidebar__link active" : "admin-sidebar__link"
          }
        >
          Quản lý phim
        </NavLink>

        <NavLink
          to="/admin/showtimes"
          className={({ isActive }) =>
            isActive ? "admin-sidebar__link active" : "admin-sidebar__link"
          }
        >
          Quản lý suất chiếu
        </NavLink>

        <NavLink
          to="/admin/bookings"
          className={({ isActive }) =>
            isActive ? "admin-sidebar__link active" : "admin-sidebar__link"
          }
        >
          Quản lý đặt vé
        </NavLink>
      </nav>
    </aside>
  );
}
```

---

### 3.3. `src/layouts/AdminLayout.tsx`

```tsx
import { Outlet } from "react-router-dom";
import AdminSidebar from "../components/admin/AdminSidebar";
import "../styles/admin.css";

export default function AdminLayout() {
  const user = JSON.parse(localStorage.getItem("user") || "{}");

  return (
    <div className="admin-layout">
      <AdminSidebar />

      <main className="admin-main">
        <header className="admin-header">
          <div>
            <h1>Trang quản trị</h1>
            <p>Quản lý dữ liệu hệ thống đặt vé xem phim</p>
          </div>

          <div className="admin-user">
            <span>{user.fullName || user.email || "Admin"}</span>
            <strong>{user.role || "ADMIN"}</strong>
          </div>
        </header>

        <section className="admin-content">
          <Outlet />
        </section>
      </main>
    </div>
  );
}
```

---

### 3.4. `src/pages/admin/AdminDashboardPage.tsx`

```tsx
export default function AdminDashboardPage() {
  return (
    <div className="admin-page">
      <h2>Dashboard</h2>

      <div className="admin-stats">
        <div className="admin-card">
          <span>Tổng phim</span>
          <strong>24</strong>
        </div>

        <div className="admin-card">
          <span>Suất chiếu</span>
          <strong>56</strong>
        </div>

        <div className="admin-card">
          <span>Đơn đặt vé</span>
          <strong>128</strong>
        </div>

        <div className="admin-card">
          <span>Đơn đã thanh toán</span>
          <strong>96</strong>
        </div>
      </div>

      <div className="admin-section">
        <h3>Tổng quan hệ thống</h3>
        <p>
          Đây là trang tổng quan dành cho quản trị viên. Admin có thể theo dõi
          số lượng phim, suất chiếu, đơn đặt vé và trạng thái thanh toán trong
          hệ thống.
        </p>
      </div>
    </div>
  );
}
```

---

### 3.5. `src/pages/admin/AdminMoviesPage.tsx`

```tsx
export default function AdminMoviesPage() {
  return (
    <div className="admin-page">
      <h2>Quản lý phim</h2>

      <div className="admin-section">
        <p>
          Trang này dùng để quản lý danh sách phim, thêm phim mới, chỉnh sửa
          thông tin phim, cập nhật trạng thái đang chiếu hoặc sắp chiếu.
        </p>

        <button className="admin-button">Thêm phim mới</button>
      </div>
    </div>
  );
}
```

---

### 3.6. `src/pages/admin/AdminShowtimesPage.tsx`

```tsx
export default function AdminShowtimesPage() {
  return (
    <div className="admin-page">
      <h2>Quản lý suất chiếu</h2>

      <div className="admin-section">
        <p>
          Trang này dùng để quản lý lịch chiếu phim, bao gồm phim, phòng chiếu,
          ngày chiếu, giờ chiếu và giá vé.
        </p>

        <button className="admin-button">Thêm suất chiếu</button>
      </div>
    </div>
  );
}
```

---

### 3.7. `src/pages/admin/AdminBookingsPage.tsx`

```tsx
export default function AdminBookingsPage() {
  return (
    <div className="admin-page">
      <h2>Quản lý đặt vé</h2>

      <div className="admin-section">
        <p>
          Trang này dùng để theo dõi danh sách đơn đặt vé, trạng thái thanh
          toán, trạng thái ghế và chi tiết vé của người dùng.
        </p>

        <button className="admin-button">Xem danh sách đơn</button>
      </div>
    </div>
  );
}
```

---

### 3.8. Cập nhật `src/App.tsx`

```tsx
import { BrowserRouter, Routes, Route } from "react-router-dom";

import AdminRouteGuard from "./routes/AdminRouteGuard";
import AdminLayout from "./layouts/AdminLayout";

import AdminDashboardPage from "./pages/admin/AdminDashboardPage";
import AdminMoviesPage from "./pages/admin/AdminMoviesPage";
import AdminShowtimesPage from "./pages/admin/AdminShowtimesPage";
import AdminBookingsPage from "./pages/admin/AdminBookingsPage";

function HomePage() {
  return <h1>Trang chủ</h1>;
}

function LoginPage() {
  const loginAsAdmin = () => {
    localStorage.setItem(
      "user",
      JSON.stringify({
        id: 1,
        fullName: "Admin Demo",
        email: "admin@gmail.com",
        role: "ADMIN",
      })
    );

    window.location.href = "/admin";
  };

  const loginAsUser = () => {
    localStorage.setItem(
      "user",
      JSON.stringify({
        id: 2,
        fullName: "User Demo",
        email: "user@gmail.com",
        role: "USER",
      })
    );

    window.location.href = "/";
  };

  return (
    <div style={{ padding: 24 }}>
      <h1>Đăng nhập demo</h1>
      <button onClick={loginAsAdmin}>Đăng nhập Admin</button>
      <button onClick={loginAsUser} style={{ marginLeft: 12 }}>
        Đăng nhập User
      </button>
    </div>
  );
}

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/login" element={<LoginPage />} />

        <Route element={<AdminRouteGuard />}>
          <Route path="/admin" element={<AdminLayout />}>
            <Route index element={<AdminDashboardPage />} />
            <Route path="movies" element={<AdminMoviesPage />} />
            <Route path="showtimes" element={<AdminShowtimesPage />} />
            <Route path="bookings" element={<AdminBookingsPage />} />
          </Route>
        </Route>
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

---

### 3.9. `src/styles/admin.css`

```css
* {
  box-sizing: border-box;
}

.admin-layout {
  min-height: 100vh;
  display: flex;
  background: #f5f7fb;
  color: #1f2937;
}

.admin-sidebar {
  width: 260px;
  min-height: 100vh;
  background: #111827;
  color: #ffffff;
  padding: 24px 18px;
}

.admin-sidebar__logo {
  margin-bottom: 32px;
}

.admin-sidebar__logo h2 {
  margin: 0;
  font-size: 22px;
}

.admin-sidebar__logo span {
  display: block;
  margin-top: 6px;
  font-size: 13px;
  color: #9ca3af;
}

.admin-sidebar__nav {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.admin-sidebar__link {
  display: block;
  padding: 12px 14px;
  border-radius: 10px;
  color: #d1d5db;
  text-decoration: none;
  font-size: 15px;
  transition: 0.2s;
}

.admin-sidebar__link:hover {
  background: #1f2937;
  color: #ffffff;
}

.admin-sidebar__link.active {
  background: #2563eb;
  color: #ffffff;
}

.admin-main {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.admin-header {
  height: 88px;
  padding: 20px 32px;
  background: #ffffff;
  border-bottom: 1px solid #e5e7eb;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.admin-header h1 {
  margin: 0;
  font-size: 24px;
}

.admin-header p {
  margin: 6px 0 0;
  color: #6b7280;
  font-size: 14px;
}

.admin-user {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 4px;
}

.admin-user span {
  font-size: 14px;
}

.admin-user strong {
  font-size: 13px;
  color: #2563eb;
}

.admin-content {
  padding: 32px;
}

.admin-page h2 {
  margin-top: 0;
  margin-bottom: 20px;
  font-size: 26px;
}

.admin-stats {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
  margin-bottom: 28px;
}

.admin-card {
  background: #ffffff;
  border-radius: 16px;
  padding: 24px;
  border: 1px solid #e5e7eb;
  box-shadow: 0 8px 20px rgba(15, 23, 42, 0.05);
}

.admin-card span {
  display: block;
  color: #6b7280;
  margin-bottom: 10px;
  font-size: 14px;
}

.admin-card strong {
  font-size: 30px;
  color: #111827;
}

.admin-section {
  background: #ffffff;
  border-radius: 16px;
  padding: 24px;
  border: 1px solid #e5e7eb;
}

.admin-section h3 {
  margin-top: 0;
}

.admin-section p {
  line-height: 1.6;
  color: #4b5563;
}

.admin-button {
  margin-top: 16px;
  padding: 10px 18px;
  border: none;
  border-radius: 10px;
  background: #2563eb;
  color: #ffffff;
  cursor: pointer;
  font-weight: 600;
}

.admin-button:hover {
  background: #1d4ed8;
}

@media (max-width: 900px) {
  .admin-layout {
    flex-direction: column;
  }

  .admin-sidebar {
    width: 100%;
    min-height: auto;
  }

  .admin-stats {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 600px) {
  .admin-header {
    height: auto;
    flex-direction: column;
    align-items: flex-start;
    gap: 12px;
  }

  .admin-user {
    align-items: flex-start;
  }

  .admin-stats {
    grid-template-columns: 1fr;
  }
}
```

---

## 4. Cách kiểm tra

### Trường hợp 1: Chưa đăng nhập

Truy cập:

```txt
/admin
```

Kết quả mong muốn:

```txt
Bị chuyển hướng về /login
```

---

### Trường hợp 2: Đăng nhập bằng tài khoản USER

Dữ liệu mẫu:

```json
{
  "id": 2,
  "fullName": "User Demo",
  "email": "user@gmail.com",
  "role": "USER"
}
```

Truy cập:

```txt
/admin
```

Kết quả mong muốn:

```txt
Bị chuyển hướng về /
```

---

### Trường hợp 3: Đăng nhập bằng tài khoản ADMIN

Dữ liệu mẫu:

```json
{
  "id": 1,
  "fullName": "Admin Demo",
  "email": "admin@gmail.com",
  "role": "ADMIN"
}
```

Truy cập:

```txt
/admin
/admin/movies
/admin/showtimes
/admin/bookings
```

Kết quả mong muốn:

```txt
Admin truy cập được layout quản trị và các route con.
Sidebar điều hướng hoạt động.
```

---

## 5. Kết quả đạt được

* Đã tạo `AdminLayout`.
* Đã tạo `AdminSidebar`.
* Đã tạo `AdminDashboardPage`.
* Đã tạo `AdminRouteGuard`.
* Đã chặn user thường truy cập admin.
* Đã chuyển hướng user không đủ quyền.
* Đã tạo route `/admin`.
* Đã tạo route con:

  * `/admin/movies`
  * `/admin/showtimes`
  * `/admin/bookings`

---

## 6. Commit đề xuất

```bash
git add .
git commit -m "feat(admin): build admin layout and route guard"
```

Hoặc nếu muốn tự động liên kết issue:

```bash
git commit -m "feat(admin): build admin layout and route guard - Closes #60"
```

---

## 7. Ghi chú nộp issue

Issue #60 hiện chưa thấy nhánh hoặc pull request liên quan. Khi nộp bài nên tạo branch riêng và mở PR có ghi:

```txt
Closes #60
```

Ví dụ tên branch:

```bash
git checkout -b feat/admin-layout-route-guard
```
