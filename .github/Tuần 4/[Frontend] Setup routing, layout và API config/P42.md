# Issue #42 - [Frontend] Setup routing, layout và API config

## 1. Thông tin task

**Issue:** #42
**Tên task:** [Frontend] Setup routing, layout và API config
**Người phụ trách:** JayphamX
**Mục tiêu:** Cấu hình routing, layout chung và kết nối API cho frontend.

---

## 2. Danh sách công việc

* [x] Tạo `AppRoutes`
* [x] Tạo `MainLayout`
* [x] Tạo `Navbar`
* [x] Tạo `Footer`
* [x] Cấu hình Axios
* [x] Cấu hình `.env` cho API base URL
* [x] Tạo route cho các trang chính

---

## 3. Cấu trúc thư mục đề xuất

```txt
src/
├── api/
│   └── axiosClient.js
├── components/
│   ├── Footer.jsx
│   └── Navbar.jsx
├── layouts/
│   └── MainLayout.jsx
├── pages/
│   ├── HomePage.jsx
│   ├── MoviesPage.jsx
│   ├── MovieDetailPage.jsx
│   ├── LoginPage.jsx
│   ├── RegisterPage.jsx
│   └── NotFoundPage.jsx
├── routes/
│   └── AppRoutes.jsx
├── App.jsx
├── main.jsx
└── index.css
```

---

## 4. Cài thư viện cần thiết

```bash
npm install react-router-dom axios
```

---

## 5. File `.env`

Tạo file `.env` ở thư mục gốc project:

```env
VITE_API_BASE_URL=http://localhost:3000/api
```

> Nếu backend chạy port khác thì sửa lại URL cho đúng.

---

## 6. Code cấu hình Axios

### `src/api/axiosClient.js`

```js
import axios from "axios";

const axiosClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  headers: {
    "Content-Type": "application/json",
  },
});

// Tự động gắn token nếu đã đăng nhập
axiosClient.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem("accessToken");

    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }

    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// Xử lý lỗi response chung
axiosClient.interceptors.response.use(
  (response) => response.data,
  (error) => {
    console.error("API Error:", error.response?.data || error.message);
    return Promise.reject(error);
  }
);

export default axiosClient;
```

---

## 7. Code Navbar

### `src/components/Navbar.jsx`

```jsx
import { Link, NavLink } from "react-router-dom";

function Navbar() {
  return (
    <header className="navbar">
      <div className="navbar-container">
        <Link to="/" className="logo">
          Cinema Booking
        </Link>

        <nav className="nav-links">
          <NavLink to="/">Trang chủ</NavLink>
          <NavLink to="/movies">Phim</NavLink>
          <NavLink to="/login">Đăng nhập</NavLink>
          <NavLink to="/register">Đăng ký</NavLink>
        </nav>
      </div>
    </header>
  );
}

export default Navbar;
```

---

## 8. Code Footer

### `src/components/Footer.jsx`

```jsx
function Footer() {
  return (
    <footer className="footer">
      <p>© 2026 Cinema Booking. All rights reserved.</p>
    </footer>
  );
}

export default Footer;
```

---

## 9. Code MainLayout

### `src/layouts/MainLayout.jsx`

```jsx
import { Outlet } from "react-router-dom";
import Navbar from "../components/Navbar";
import Footer from "../components/Footer";

function MainLayout() {
  return (
    <div className="app-layout">
      <Navbar />

      <main className="main-content">
        <Outlet />
      </main>

      <Footer />
    </div>
  );
}

export default MainLayout;
```

---

## 10. Code AppRoutes

### `src/routes/AppRoutes.jsx`

```jsx
import { BrowserRouter, Routes, Route } from "react-router-dom";
import MainLayout from "../layouts/MainLayout";

import HomePage from "../pages/HomePage";
import MoviesPage from "../pages/MoviesPage";
import MovieDetailPage from "../pages/MovieDetailPage";
import LoginPage from "../pages/LoginPage";
import RegisterPage from "../pages/RegisterPage";
import NotFoundPage from "../pages/NotFoundPage";

function AppRoutes() {
  return (
    <BrowserRouter>
      <Routes>
        <Route element={<MainLayout />}>
          <Route path="/" element={<HomePage />} />
          <Route path="/movies" element={<MoviesPage />} />
          <Route path="/movies/:id" element={<MovieDetailPage />} />
          <Route path="/login" element={<LoginPage />} />
          <Route path="/register" element={<RegisterPage />} />
        </Route>

        <Route path="*" element={<NotFoundPage />} />
      </Routes>
    </BrowserRouter>
  );
}

export default AppRoutes;
```

---

## 11. Code App.jsx

### `src/App.jsx`

```jsx
import AppRoutes from "./routes/AppRoutes";

function App() {
  return <AppRoutes />;
}

export default App;
```

---

## 12. Code main.jsx

### `src/main.jsx`

```jsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

---

## 13. Các trang chính

### `src/pages/HomePage.jsx`

```jsx
function HomePage() {
  return (
    <section>
      <h1>Trang chủ</h1>
      <p>Chào mừng bạn đến với hệ thống đặt vé xem phim.</p>
    </section>
  );
}

export default HomePage;
```

---

### `src/pages/MoviesPage.jsx`

```jsx
import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import axiosClient from "../api/axiosClient";

function MoviesPage() {
  const [movies, setMovies] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const fetchMovies = async () => {
      try {
        const data = await axiosClient.get("/movies");
        setMovies(data);
      } catch (error) {
        console.error("Lỗi khi lấy danh sách phim:", error);
      } finally {
        setLoading(false);
      }
    };

    fetchMovies();
  }, []);

  if (loading) {
    return <p>Đang tải danh sách phim...</p>;
  }

  return (
    <section>
      <h1>Danh sách phim</h1>

      {movies.length === 0 ? (
        <p>Chưa có phim nào.</p>
      ) : (
        <div className="movie-grid">
          {movies.map((movie) => (
            <div key={movie.id} className="movie-card">
              <h3>{movie.title || movie.name}</h3>
              <p>{movie.description || "Chưa có mô tả."}</p>
              <Link to={`/movies/${movie.id}`}>Xem chi tiết</Link>
            </div>
          ))}
        </div>
      )}
    </section>
  );
}

export default MoviesPage;
```

---

### `src/pages/MovieDetailPage.jsx`

```jsx
import { useEffect, useState } from "react";
import { useParams } from "react-router-dom";
import axiosClient from "../api/axiosClient";

function MovieDetailPage() {
  const { id } = useParams();
  const [movie, setMovie] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const fetchMovieDetail = async () => {
      try {
        const data = await axiosClient.get(`/movies/${id}`);
        setMovie(data);
      } catch (error) {
        console.error("Lỗi khi lấy chi tiết phim:", error);
      } finally {
        setLoading(false);
      }
    };

    fetchMovieDetail();
  }, [id]);

  if (loading) {
    return <p>Đang tải chi tiết phim...</p>;
  }

  if (!movie) {
    return <p>Không tìm thấy phim.</p>;
  }

  return (
    <section>
      <h1>{movie.title || movie.name}</h1>
      <p>{movie.description || "Chưa có mô tả."}</p>
      <p>Thể loại: {movie.genre || "Đang cập nhật"}</p>
      <p>Thời lượng: {movie.duration || "Đang cập nhật"}</p>
    </section>
  );
}

export default MovieDetailPage;
```

---

### `src/pages/LoginPage.jsx`

```jsx
import { useState } from "react";
import axiosClient from "../api/axiosClient";

function LoginPage() {
  const [formData, setFormData] = useState({
    email: "",
    password: "",
  });

  const handleChange = (event) => {
    const { name, value } = event.target;

    setFormData({
      ...formData,
      [name]: value,
    });
  };

  const handleSubmit = async (event) => {
    event.preventDefault();

    try {
      const data = await axiosClient.post("/auth/login", formData);

      if (data.accessToken) {
        localStorage.setItem("accessToken", data.accessToken);
      }

      alert("Đăng nhập thành công!");
    } catch (error) {
      alert("Đăng nhập thất bại!");
      console.error(error);
    }
  };

  return (
    <section>
      <h1>Đăng nhập</h1>

      <form onSubmit={handleSubmit} className="form">
        <input
          type="email"
          name="email"
          placeholder="Email"
          value={formData.email}
          onChange={handleChange}
          required
        />

        <input
          type="password"
          name="password"
          placeholder="Mật khẩu"
          value={formData.password}
          onChange={handleChange}
          required
        />

        <button type="submit">Đăng nhập</button>
      </form>
    </section>
  );
}

export default LoginPage;
```

---

### `src/pages/RegisterPage.jsx`

```jsx
import { useState } from "react";
import axiosClient from "../api/axiosClient";

function RegisterPage() {
  const [formData, setFormData] = useState({
    fullName: "",
    email: "",
    password: "",
    phone: "",
  });

  const handleChange = (event) => {
    const { name, value } = event.target;

    setFormData({
      ...formData,
      [name]: value,
    });
  };

  const handleSubmit = async (event) => {
    event.preventDefault();

    try {
      await axiosClient.post("/auth/register", formData);
      alert("Đăng ký thành công!");
    } catch (error) {
      alert("Đăng ký thất bại!");
      console.error(error);
    }
  };

  return (
    <section>
      <h1>Đăng ký</h1>

      <form onSubmit={handleSubmit} className="form">
        <input
          type="text"
          name="fullName"
          placeholder="Họ và tên"
          value={formData.fullName}
          onChange={handleChange}
          required
        />

        <input
          type="email"
          name="email"
          placeholder="Email"
          value={formData.email}
          onChange={handleChange}
          required
        />

        <input
          type="text"
          name="phone"
          placeholder="Số điện thoại"
          value={formData.phone}
          onChange={handleChange}
        />

        <input
          type="password"
          name="password"
          placeholder="Mật khẩu"
          value={formData.password}
          onChange={handleChange}
          required
        />

        <button type="submit">Đăng ký</button>
      </form>
    </section>
  );
}

export default RegisterPage;
```

---

### `src/pages/NotFoundPage.jsx`

```jsx
import { Link } from "react-router-dom";

function NotFoundPage() {
  return (
    <section>
      <h1>404 - Không tìm thấy trang</h1>
      <p>Trang bạn đang tìm kiếm không tồn tại.</p>
      <Link to="/">Quay về trang chủ</Link>
    </section>
  );
}

export default NotFoundPage;
```

---

## 14. CSS cơ bản

### `src/index.css`

```css
* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: Arial, sans-serif;
  background-color: #f5f5f5;
  color: #222;
}

a {
  text-decoration: none;
  color: inherit;
}

.app-layout {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.navbar {
  background-color: #111827;
  color: white;
  padding: 16px 32px;
}

.navbar-container {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.logo {
  font-size: 22px;
  font-weight: bold;
}

.nav-links {
  display: flex;
  gap: 20px;
}

.nav-links a {
  color: white;
}

.nav-links a.active {
  color: #facc15;
  font-weight: bold;
}

.main-content {
  flex: 1;
  padding: 32px;
}

.footer {
  background-color: #111827;
  color: white;
  text-align: center;
  padding: 16px;
}

.movie-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 20px;
}

.movie-card {
  background-color: white;
  padding: 16px;
  border-radius: 8px;
  border: 1px solid #ddd;
}

.form {
  max-width: 400px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.form input {
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 6px;
}

.form button {
  padding: 10px;
  border: none;
  border-radius: 6px;
  background-color: #111827;
  color: white;
  cursor: pointer;
}

.form button:hover {
  background-color: #1f2937;
}
```

---

## 15. Cách chạy project

```bash
npm install
npm run dev
```

Sau đó mở trình duyệt tại:

```txt
http://localhost:5173
```

---

## 16. Kiểm tra Acceptance Criteria

### Routing hoạt động đúng

Các route đã tạo:

```txt
/
 /movies
 /movies/:id
 /login
 /register
```

### Layout chung hiển thị được

`MainLayout` đã bao gồm:

```txt
Navbar
Outlet
Footer
```

### Navbar/Footer hiển thị trên trang chính

`Navbar` và `Footer` được gọi trong `MainLayout`, nên các trang chính đều hiển thị layout chung.

### Axios gọi được API backend

File `axiosClient.js` đã cấu hình:

```txt
baseURL từ VITE_API_BASE_URL
Content-Type application/json
Authorization Bearer token
Response/Error interceptor
```

---

## 17. Commit đề xuất

```bash
git add .
git commit -m "Closes #42 setup frontend routing layout and api config"
git push origin main
```

---

## 18. Nội dung comment đóng issue

```md
Đã hoàn thành Issue #42: Setup routing, layout và API config cho frontend.

Nội dung đã làm:
- Tạo AppRoutes để quản lý route chính.
- Tạo MainLayout dùng chung cho các trang.
- Tạo Navbar và Footer.
- Cấu hình Axios client để kết nối API backend.
- Cấu hình .env với VITE_API_BASE_URL.
- Tạo các route chính: trang chủ, danh sách phim, chi tiết phim, đăng nhập, đăng ký và 404.

Kiểm tra:
- Routing hoạt động đúng.
- Layout chung hiển thị được.
- Navbar/Footer hiển thị trên các trang chính.
- Axios đã sẵn sàng gọi API backend.

Commit:
Closes #42
```
